(class main
 (field x "abc")
 (method main ()
  (begin
   (print "inside main")
   (set x (call (new loops)  looper 0))
   (print "back inside main" x)
  )
 )
 )
(class loops
 (method looper (x) 
   (begin
     (print "inside looper, x = " x " and x < 100: " (< x 100))
     (while (< x 100)
       (begin
         (set x (+ x 1))
         (print "x: " x)
         (if (== x 5)
           (begin (begin (print (+ 5 2) ) (return (call (new oops) ops 1 2)) (print 5))(print 5))
         )
         (print "asef")
       )
     )
     (print "bad")
     (if (== x 5) (return 2) (return 1))
     (print "hi")
   )
 )
)
(class oops
  (method ops (a b)
    (begin
      (return (+ a b))
      )
  )
)